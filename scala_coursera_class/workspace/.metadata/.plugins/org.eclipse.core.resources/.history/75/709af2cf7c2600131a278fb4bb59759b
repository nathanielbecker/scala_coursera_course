package week_1

object session2 {

def countChange(money: Int, coins: List[Int]): Int = {

def countchangio(money: Int, coins: List[Int], numsuccesses: Int), : Int = {
	if (money == 0) 1
	else if (coins.isEmpty) 0
	else	do { countchangio(money - x*coins.head, coins.tail) ; x += 1} while (x < money/coins.head)
	
	 


}
}
}